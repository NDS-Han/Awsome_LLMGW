# Agent
bedrock-agentcore>=1.0.0
strands-agents>=0.1.0
strands-agents-tools>=0.1.0

# Data (pandas는 마이그레이션 스크립트에서 CSV 파싱용)
pandas>=2.0.0

# API Server
fastapi>=0.110.0
uvicorn>=0.27.0
pydantic>=2.0.0

# AWS (boto3 rds-data 클라이언트로 Aurora Data API 사용)
boto3>=1.34.0

# .env 자동 로드
python-dotenv>=1.0.0

# Langfuse (LLM observability)
langfuse>=2.0.0

# OpenTelemetry (ADOT — AWS 권장 자동 계측, 개별 패키지 포함)
aws-opentelemetry-distro>=0.7.0

# JWT verification (Cognito)
python-jose[cryptography]>=3.3.0

# Gateway setup (optional)
# bedrock-agentcore-starter-toolkit
